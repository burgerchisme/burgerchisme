$(document).ready(function(){
    $('#myCarousel').carousel({
        interval: 3000, // Cambia cada 3 segundos
        pause: "hover" // Pausa la animación al pasar el mouse sobre el carrusel
    });
});
